[package]
name = "vocalai-core"
version = "0.1.0"
edition = "2021"

[dependencies]
ort = { version = "=2.0.0-rc.13", features = ["ndarray"] }
ndarray = "0.17"

[features]
default = []
coreml = ["ort/coreml"]
cuda = ["ort/cuda"]
