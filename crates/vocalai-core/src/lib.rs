//! Placeholder scaffold crate. Real inference runtime lands in Milestone 1 —
//! see `docs/phase1-onnx-rust-cli-plan.md` and `docs/issues.md` (VAI-001).

#[cfg(test)]
mod tests {
    // Ignored so the scaffold commit itself can pass `make check`. Remove
    // `#[ignore]` (or delete this test) once Milestone 1 adds real coverage.
    #[test]
    #[ignore = "TDD seed placeholder — delete once Milestone 1 (VAI-001) adds real tests"]
    fn scaffold_reminder() {
        panic!("Delete this and write your first real test — see docs/issues.md VAI-001");
    }
}
