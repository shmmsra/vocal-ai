[package]
name = "vocalai-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "vocalai"
path = "src/main.rs"

[dependencies]
vocalai-core = { path = "../vocalai-core" }

[features]
default = []
coreml = ["vocalai-core/coreml"]
cuda = ["vocalai-core/cuda"]
