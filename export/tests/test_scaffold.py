import pytest


@pytest.mark.skip(reason="TDD seed placeholder — delete once Milestone 1 (VAI-001) adds real tests")
def test_scaffold_reminder():
    """Replace with a real test before starting Milestone 1 exports — see docs/issues.md VAI-001."""
    assert False, "Delete this and write your first real test"
